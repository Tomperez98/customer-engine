export type Example = {
    org_code: string
    example_id: string
    automatic_response_id: string
    example: string
}

export enum ExampleKeys {
    org_code = 'Código de organización',
    example_id = 'ID',
    automatic_response_id = 'ID formulario',
    example = 'ejemplo',
}
